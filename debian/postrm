#!/bin/sh
# postrm script for flexplorer

set -e

if [ -f /usr/lib/flexplorer/composer.lock ] ; then
    rm -f /usr/lib/flexplorer/composer.lock
fi

if [ -f /var/lib/flexplorer/autoload.php ] ; then
    rm -rf /var/lib/flexplorer/
fi


#DEBHELPER#

exit 0
